# ai_explore

explore codex functions

## NVDA Time Series Forecast

`nvda_forecast.py` downloads daily NVDA prices, builds forecasting models with extra features, and back-tests them on the current year's data. It saves an interactive Plotly chart to `nvda_forecast.html` and prints each model's error to the console.

### Usage

```bash
python nvda_forecast.py
```
